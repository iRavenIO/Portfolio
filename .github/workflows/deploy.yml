name: Deploy to Portainer

on:
  push:
    branches:
      - master

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: koushamad/kousha.dev:latest

#      - name: Deploy to Portainer
#        run: |
#          curl -X POST "https://portainer.kousha.dev/api/stacks?endpointId=1&type=2" \
#            -H "accept: application/json" \
#            -H "Authorization: Bearer ${{ secrets.PORTAINER_TOKEN }}" \
#            -d "{
#                'Name': 'your-stack-name',
#                'StackFileContent': 'version: \"3.1\"\nservices:\n  web:\n    image: koushamad/kousha.dev:latest\n    ports:\n      - \"8080:80\"'
#              }"