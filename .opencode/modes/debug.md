# Mode: Debug

Use when: failures or regressions.
Tools: Read/Grep, Bash for repro, MCP git/fs.
Tool priority: MCP first, Bash fallback.
Edits: allowed.
Output: root cause, fix, verification steps.
Required reporting:
- Pre: mode + repro plan
- During: reproduction status + findings
- Post: root cause + fix + verification
